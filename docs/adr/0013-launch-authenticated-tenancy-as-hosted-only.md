# Launch authenticated tenancy as hosted-only

The authenticated beta offers one server-authoritative hosted private workspace with an account-scoped cache, not a parallel local-only mode. A local-only choice could preserve maximal device ownership, but supporting two ownership and synchronization models during the tenancy cutover would multiply migration, privacy, recovery, and testing paths before the hosted model is validated.
