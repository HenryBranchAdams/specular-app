# Start authenticated tenancy with clean data

The authenticated beta starts with empty author-account workspaces and removes existing unowned local test data and unowned test snapshots at cutover. An automatic claim or import could preserve experiments, but those records have no verified owner and the current material is disposable; workspace archives remain download-only and import is deferred.
